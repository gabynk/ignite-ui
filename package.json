{
  "name": "gnakassima-ignite-ui",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "npm@10.5.0",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "turbo run build --filter=!@gnakassima-ignite-ui/docs && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.7",
    "@vitejs/plugin-react": "^4.3.1",
    "turbo": "^2.0.5",
    "vite": "^5.3.1"
  }
}
